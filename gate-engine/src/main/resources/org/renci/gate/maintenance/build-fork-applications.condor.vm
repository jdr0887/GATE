# generated by the PortalGlideinFactory

universe             = grid

grid_resource        = ${gridType} ${gatekeeper}/jobmanager-fork
globusrsl            = ${rsl}
x509userproxy        = ${workDirPath}/.proxy

executable           = build-fork-applications.sh

TransferExecutable   = true
stream_output        = False
stream_error         = False
WhenToTransferOutput = ON_EXIT

output               = build-fork-applications.out
error                = build-fork-applications.err
log                  = build-fork-applications.log

periodic_hold        = ( (GlobusStatus==16) \
                         || ((JobStatus==1) && ((CurrentTime - EnteredCurrentStatus) > (12*60*60))) \
                         || ((JobStatus==2) && ((CurrentTime - EnteredCurrentStatus) > (24*60*60))) \
                       )
periodic_remove      = (NumSystemHolds >= 1)

notification         = NEVER

queue 1
